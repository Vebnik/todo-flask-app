## Launch

### - create venv
`python (py|python3) -m venv venv`

### - activate venv
`(source) ./venv/(bin|scripts)/activate(.ps1)`

### - install deps
`pip install -r req.txt`

### - start app
`flask run --debug`